import { log } from "console";

const language: string = "Typescript"

function hello() {
    log(`\n \n`);
    log(`******************************`);
    log(`* I'm learning ${language} ****`);
    log(`******************************`);
    log(`\n \n`);
}

hello();